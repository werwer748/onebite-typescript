{
  "include": ["src"],
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    "outDir": "dist",
    /*
     * 기본적으로 strict 옵션은 strictXXX 옵션들의 상위 옵션이다.
     * 따라서, strict 옵션을 true로 설정하면 strictXXX 옵션들이 모두 true로 설정된다.
     * 필요에 따라 strict 옵션을 true로 설정하고, strictXXX 옵션들만 false로 설정할 수 있다.
     */
    "strict": true,
    "moduleDetection": "force",
    "skipLibCheck": true

    // 옵션을 false로 설정 - 어떤 타입이 지정된 변수에라도  null, undefined를 허용
    // "strictNullChecks": false
  }
}
